/*const {calculate, farnhitToClesius, clesiousToFarnhit, add} = require('../math.js');


test('test calculater function', () => {
    expect(calculate(100, .2)).toBe(120);
});

test('test farnhit to celize funciton', () => {
    expect(farnhitToClesius(32)).toBe(0);
});

test('test celize to farnhit funciton', () => {
    expect(clesiousToFarnhit(0)).toBe(32);
});
//using done to let jest module know that the function which tested is async funciton 
test('test async function using done', (done) => {
    add(10, 10).then(result => {
        expect(result).toBe(20);
        done();
    });
});

test('test async function async/await', async () => {
    const sum = await add(10, 15);
    expect(sum).toBe(25);
});*/